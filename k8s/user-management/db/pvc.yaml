apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-management-mysql-pvc  
spec:
  storageClassName: "standard"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi