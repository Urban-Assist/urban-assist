apiVersion: v1
kind: ConfigMap
metadata:
  name: react-nginx-conf
data:
  default.conf: |
    server {
      listen 80;
      server_name _;
      root /usr/share/nginx/html;
      
      location / {
        try_files $uri $uri/ /index.html;
      }
    }
